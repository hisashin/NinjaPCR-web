extends ../layout

block navbar
  +navbar('/maker/misc/')

block sidebar
  +sidebar('/maker/misc/')

block main

  - var dir = 'maker'
  - var subdir = 'misc'

  +itemhead(dir, subdir)

    +itemtitle(dir, subdir, 0)

    p
      | #{$i18n[dir][subdir].items[0].text1}
      +table($i18n[dir][subdir].items[0].table)

  +nextstep('/maker/assy/index.html', $i18n.maker.assy.label)

